namespace Maximagus.Scripts.Spells
{
    public enum DamageType
    {
        Fire,
        Frost,
        Physical
    }

    public enum ContextProperty
    {
        FireDamageDealt,
        FireInstances
    }
}